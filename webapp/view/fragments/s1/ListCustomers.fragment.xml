<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core" 
>
     <List
            visible="{= ${s1Model>/iconTab/selectedKey} === 'jsonModel' || !${s1Model>/iconTab/selectedKey} }" 
            id="listCustomers"
            items="{
                path: '/data/customers',
                model: 'myData',
                sorter: [{
                    path: 'name', 
                    descending: true
                }], 
                filters: {
                    and: false,
                    filters: [{
                        path: 'adress/country', 
                        operator: 'EQ', 
                        value1: 'BR'
                    }, 
                    {
                        path: 'adress/country', 
                        operator: 'EQ', 
                        value1: 'US'
                    }]
                }
            }">
            <headerToolbar>
                <Toolbar>
                        <Title
                            text="{i18n>title}"
                        />
                        <ToolbarSpacer />
                        <Button text="{i18n>s1.button.listAction}" 
                            icon="sap-icon://refresh"
                            type="Emphasized"
                            press=".onListAction" />
                </Toolbar>
            </headerToolbar>
            <ObjectListItem title="{myData>name}">
                <attributes>
                    <ObjectAttribute
                        text="{myData>id}" />
                </attributes>
                <firstStatus>
                    <ObjectStatus
                        text="{
                            path: 'myData>adress/region', 
                            type: 'sap.ui.model.type.String'
                        }"
                        state="{
                            parts: [{ path: 'myData>adress/region' }, { path: 'myData>adress/country' }], 
                            formatter: '.oFormatter.formatValueState'
                        }" />
                </firstStatus>
            </ObjectListItem>
        </List>
</c:FragmentDefinition>